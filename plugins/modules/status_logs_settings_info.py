#!/usr/bin/python
###############################################################
# DO NOT EDIT THIS FILE DIRECTLY. THIS FILE IS AUTO-GENERATED.
# BY tools/module_generator.py BASED ON THE REST API SCHEMA IF
# YOU NEED TO MAKE CHANGES, PLEASE EDIT THE TEMPLATE OR THE
# GENERATOR SCRIPT. ALTERNATIVELY, YOU CAN OVERRIDE THE AUTO
# GENERATED MODULE BY ADDING THIS MODULES NAME TO THE
# tools/generator.yml FILE.
###############################################################
"""An Ansible module for interacting with /api/v2/status/logs/settings."""

# pylint: disable=too-many-lines,duplicate-code

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.pfrest.pfsense.plugins.module_utils import base, rest

DOCUMENTATION = r"""
module: status_logs_settings_info
description:
- Retrieve information about the Log Settings.
short_description: Retrieve information about the Log Settings.
requirements:
- pfSense package L(pfSense-pkg-RESTAPI,https://pfrest.org) must be installed on the
  target system.
options:
  api_host:
    type: str
    required: true
    description: The hostname or IP address of the pfSense device.
  api_port:
    type: int
    default: 443
    description: The port number of the pfSense API.
  api_protocol:
    type: str
    default: https
    choices:
    - http
    - https
    description: The protocol to use when connecting to the pfSense API.
  api_username:
    type: str
    default: admin
    description: The username to authenticate with the pfSense API.
  api_password:
    type: str
    default: pfsense
    description: The password to authenticate with the pfSense API.
  api_key:
    type: str
    description: An API key to use for authentication.
  validate_certs:
    type: bool
    default: true
    description: Whether to validate SSL certificates when connecting to the API.
  lookup_params:
    type: dict
    description: Parameters to lookup the specific resource to retrieve.
author:
- Jared Hendrickson (@jaredhendrickson13)

"""

EXAMPLES = """
- name: Retrieve Log Settings
  pfrest.pfsense.status_logs_settings_info:
    api_host: pfsense.example.com
    api_username: admin
    api_password: pfsense
    lookup_params: {}

"""

RETURN = """
changed:
  description: Whether any changes were made.
  type: bool
  returned: always
status:
  description: The HTTP status code of the API response.
  type: int
  returned: always
response_id:
  description: The unique response/error ID from the API.
  type: str
  returned: always
msg:
  description: A status message from the API.
  type: str
  returned: always
data:
  description: The Log Settings data returned by the API.
  type: dict
  returned: always
  contains:
    format:
      description: The format of the log entries.
      type: str
      returned: always
    reverseorder:
      description: Reverse the order of log entries.
      type: bool
      returned: always
    nentries:
      description: The number of log entries to display in the UI.
      type: int
      returned: always
    nologdefaultblock:
      description: Do not log packets that are blocked by the implicit default block
        rule.
      type: bool
      returned: always
    nologdefaultpass:
      description: Do not log packets that are allowed by the implicit default pass
        rule.
      type: bool
      returned: always
    nologbogons:
      description: Log packets blocked by Block Bogon Networks rules.
      type: bool
      returned: always
    nologprivatenets:
      description: Log packets blocked by Block Private Networks rules.
      type: bool
      returned: always
    nolognginx:
      description: Do not Log errors from the web server process.
      type: bool
      returned: always
    rawfilter:
      description: Display logs in the UI as they are generated by the packet filter,
        without any formatting.
      type: bool
      returned: always
    disablelocallogging:
      description: 'Disable writing log entries to the local disk. WARNING: This will
        also disable Login Protection!'
      type: bool
      returned: always
    logconfigchanges:
      description: Log changes made to the pfSense configuration.
      type: bool
      returned: always
    filterdescriptions:
      description: Display filter descriptions in the log entries Use `0` to not load
        descriptions, `1` to display descriptions in their own column, or `2` to display
        the description in a second row.
      type: int
      returned: always
    logfilesize:
      description: The maximum size of the log file in kilobytes.
      type: int
      returned: always
    rotatecount:
      description: The number of log file rotations to keep.
      type: int
      returned: always
    logcompressiontype:
      description: The type of compression to use for log files.
      type: str
      returned: always
    enableremotelogging:
      description: Enable remote logging.
      type: bool
      returned: always
    ipprotocol:
      description: The IP protocol to use for remote logging.
      type: str
      returned: always
    sourceip:
      description: The interface to use as the source IP address for remote logging.
      type: str
      returned: always
    remoteserver:
      description: The first remote syslog server to send log entries to.
      type: str
      returned: always
    remoteserver2:
      description: The second remote syslog server to send log entries to.
      type: str
      returned: always
    remoteserver3:
      description: The third remote syslog server to send log entries to.
      type: str
      returned: always
    logall:
      description: Log everything to the remote syslog server(s).
      type: bool
      returned: always
    filter:
      description: Log filter events to the remote syslog server(s).
      type: bool
      returned: always
    dhcp:
      description: Log DHCP events to the remote syslog server(s).
      type: bool
      returned: always
    auth:
      description: Log authentication events to the remote syslog server(s).
      type: bool
      returned: always
    portalauth:
      description: Log captive portal authentication events to the remote syslog server(s).
      type: bool
      returned: always
    vpn:
      description: Log VPN events to the remote syslog server(s).
      type: bool
      returned: always
    dpinger:
      description: Log gateway monitoring events to the remote syslog server(s).
      type: bool
      returned: always
    hostapd:
      description: Log wireless authentication events to the remote syslog server(s).
      type: bool
      returned: always
    system:
      description: Log system events to the remote syslog server(s).
      type: bool
      returned: always
    resolver:
      description: Log DNS resolver events to the remote syslog server(s).
      type: bool
      returned: always
    ppp:
      description: Log PPP events to the remote syslog server(s).
      type: bool
      returned: always
    routing:
      description: Log routing events to the remote syslog server(s).
      type: bool
      returned: always
    ntpd:
      description: Log NTP events to the remote syslog server(s).
      type: bool
      returned: always

"""


def run_module():
    """Runs this info module against /api/v2/status/logs/settings."""

    module_args = {
        "api_host": {
            "type": "str",
            "required": True,
            "no_log": False,
        },
        "api_port": {
            "type": "int",
            "required": False,
            "no_log": False,
            "default": 443,
        },
        "api_protocol": {
            "type": "str",
            "required": False,
            "no_log": False,
            "default": "https",
            "choices": ["http", "https"],
        },
        "api_username": {
            "type": "str",
            "required": False,
            "no_log": False,
            "default": "admin",
        },
        "api_password": {
            "type": "str",
            "required": False,
            "no_log": True,
            "default": "pfsense",
        },
        "api_key": {
            "type": "str",
            "required": False,
            "no_log": True,
        },
        "validate_certs": {
            "type": "bool",
            "required": False,
            "no_log": False,
            "default": True,
        },
        "lookup_params": {
            "type": "dict",
            "required": False,
            "no_log": False,
        },
    }

    module = AnsibleModule(argument_spec=module_args, supports_check_mode=True)

    client = rest.RestClient(
        host=module.params["api_host"],
        port=module.params["api_port"],
        scheme=module.params["api_protocol"],
        username=module.params["api_username"],
        password=module.params["api_password"],
        api_key=module.params.get("api_key", ""),
        validate_certs=module.params["validate_certs"],
    )

    base_module = base.BaseModule("/api/v2/status/logs/settings", client)
    changed = False
    resp = base_module.lookup_object(lookup_params=module.params["lookup_params"])

    # Capture the response message and clear it (prevent duplicate message/msg in result)
    message = resp.get("message", "")
    if "message" in resp:
        del resp["message"]

    # If the result was unsuccessful, fail the tasks with the error message returned from the API
    if "code" not in resp or resp["code"] != 200:
        module.fail_json(msg=message, **resp)

    result = {"changed": changed, "msg": "Successfully completed API request.", **resp}
    module.exit_json(**result)


if __name__ == "__main__":
    run_module()
