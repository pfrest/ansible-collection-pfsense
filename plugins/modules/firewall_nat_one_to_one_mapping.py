#!/usr/bin/python
###############################################################
# DO NOT EDIT THIS FILE DIRECTLY. THIS FILE IS AUTO-GENERATED.
# BY tools/module_generator.py BASED ON THE REST API SCHEMA IF
# YOU NEED TO MAKE CHANGES, PLEASE EDIT THE TEMPLATE OR THE
# GENERATOR SCRIPT. ALTERNATIVELY, YOU CAN OVERRIDE THE AUTO
# GENERATED MODULE BY ADDING THIS MODULES NAME TO THE
# tools/generator.yml FILE.
###############################################################

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.pfrest.pfsense.plugins.module_utils import base, rest

DOCUMENTATION = '''
module: firewall_nat_one_to_one_mapping
description:
- Manage individual 1:1 NAT mappings.
short_description: Manage individual 1:1 NAT mappings.
options:
  api_host:
    type: str
    required: true
    description: The hostname or IP address of the pfSense device.
  api_port:
    type: int
    default: 443
    description: The port number of the pfSense API.
  api_username:
    type: str
    default: admin
    description: The username to authenticate with the pfSense API.
  api_password:
    type: str
    default: pfsense
    no_log: true
    description: The password to authenticate with the pfSense API.
  api_key:
    type: str
    no_log: true
    description: An optional API key for authentication instead of username/password.
  validate_certs:
    type: bool
    default: true
    description: Whether to validate SSL certificates when connecting to the API.
  state:
    type: str
    choices:
    - present
    - absent
    default: present
    description: Whether the resource should be present or absent.
  lookup_fields:
    type: list
    elements: str
    required: false
    default: null
    description: The list of fields to use when looking up existing resources. This
      should be a list of field names that uniquely identify a resource. If not specified,
      the module will attempt to use the 'id' field if it exists, or all fields marked
      as 'unique' in the schema.
  interface:
    required: true
    type: str
    default: null
    choices: []
    description: The interface this 1:1 NAT mapping applies to.
  disabled:
    required: false
    type: bool
    default: false
    choices: []
    description: Disables this 1:1 NAT mapping.
  nobinat:
    required: false
    type: bool
    default: false
    choices: []
    description: Exclude traffic matching this mapping from a later, more general,
      mapping.
  natreflection:
    required: false
    type: str
    default: null
    choices:
    - enable
    - disable
    description: Enables or disables NAT reflection for traffic matching this mapping.
      Set to `null` to use the system default.
  ipprotocol:
    required: false
    type: str
    default: inet
    choices:
    - inet
    - inet6
    description: The IP version this mapping applies to.
  external:
    required: true
    type: str
    default: null
    choices: []
    description: 'The external IP address or interface for the 1:1 mapping. Valid
      value options are: an IP address. For interface values, the `:ip` modifier can
      be appended to the value to use the interface''s IP address instead of its entire
      subnet.'
  source:
    required: true
    type: str
    default: null
    choices: []
    description: 'The source IP address or subnet that traffic must match to apply
      this mapping. Valid value options are: an existing interface, an IP address,
      a subnet CIDR, `any`, `l2tp`, `pppoe`. The context of this address can be inverted
      by prefixing the value with `!`. For interface values, the `:ip` modifier can
      be appended to the value to use the interface''s IP address instead of its entire
      subnet.'
  destination:
    required: true
    type: str
    default: null
    choices: []
    description: 'The destination IP address or subnet that traffic must match to
      apply this mapping. Valid value options are: an existing interface, an IP address,
      a subnet CIDR, an existing alias, `any`, `l2tp`, `pppoe`. The context of this
      address can be inverted by prefixing the value with `!`. For interface values,
      the `:ip` modifier can be appended to the value to use the interface''s IP address
      instead of its entire subnet.'
  descr:
    required: false
    type: str
    default: ''
    choices: []
    description: A description for this 1:1 NAT mapping
author:
- Jared Hendrickson (@jaredhendrickson13)

'''

EXAMPLES = '''
- name: Create 1:1 NAT mapping
  pfrest.pfsense.firewall_nat_one_to_one_mapping:
    api_host: pfsense.example.com
    api_username: admin
    api_password: pfsense
    state: present
    interface: example
    external: example
    source: example
    destination: example
- name: Delete 1:1 NAT mapping
  pfrest.pfsense.firewall_nat_one_to_one_mapping:
    api_host: pfsense.example.com
    api_username: admin
    api_password: pfsense
    state: absent
    interface: example
    external: example
    source: example
    destination: example

'''

RETURNS = '''
changed:
  description: Whether any changes were made.
  type: bool
  returned: always
status:
  description: The HTTP status code of the API response.
  type: int
  returned: always
response_id:
  description: The unique response/error ID from the API.
  type: str
  returned: always
msg:
  description: A status message from the API.
  type: str
  returned: always
data:
  description: The 1:1 NAT mapping data returned by the API.
  type: dict
  returned: always
  contains:
    interface:
      description: The interface this 1:1 NAT mapping applies to.
      type: str
      returned: always
    disabled:
      description: Disables this 1:1 NAT mapping.
      type: bool
      returned: always
    nobinat:
      description: Exclude traffic matching this mapping from a later, more general,
        mapping.
      type: bool
      returned: always
    natreflection:
      description: Enables or disables NAT reflection for traffic matching this mapping.
        Set to `null` to use the system default.
      type: str
      returned: always
    ipprotocol:
      description: The IP version this mapping applies to.
      type: str
      returned: always
    external:
      description: 'The external IP address or interface for the 1:1 mapping. Valid
        value options are: an IP address. For interface values, the `:ip` modifier
        can be appended to the value to use the interface''s IP address instead of
        its entire subnet.'
      type: str
      returned: always
    source:
      description: 'The source IP address or subnet that traffic must match to apply
        this mapping. Valid value options are: an existing interface, an IP address,
        a subnet CIDR, `any`, `l2tp`, `pppoe`. The context of this address can be
        inverted by prefixing the value with `!`. For interface values, the `:ip`
        modifier can be appended to the value to use the interface''s IP address instead
        of its entire subnet.'
      type: str
      returned: always
    destination:
      description: 'The destination IP address or subnet that traffic must match to
        apply this mapping. Valid value options are: an existing interface, an IP
        address, a subnet CIDR, an existing alias, `any`, `l2tp`, `pppoe`. The context
        of this address can be inverted by prefixing the value with `!`. For interface
        values, the `:ip` modifier can be appended to the value to use the interface''s
        IP address instead of its entire subnet.'
      type: str
      returned: always
    descr:
      description: A description for this 1:1 NAT mapping
      type: str
      returned: always

'''


def run_module():
    module_args = {
        "api_host": {
            "type": str,
            "required": True,
            "default": None,
            "choices": [],
        },
        "api_port": {
            "type": int,
            "required": False,
            "default": 443,
            "choices": [],
        },
        "api_username": {
            "type": str,
            "required": False,
            "default": 'admin',
            "choices": [],
        },
        "api_password": {
            "type": str,
            "required": False,
            "default": 'pfsense',
            "choices": [],
        },
        "api_key": {
            "type": str,
            "required": False,
            "default": None,
            "choices": [],
        },
        "validate_certs": {
            "type": bool,
            "required": False,
            "default": True,
            "choices": [],
        },
        "state": {
            "type": str,
            "required": False,
            "default": 'present',
            "choices": ['present', 'absent'],
        },
        "lookup_fields": {
            "type": list,
            "required": False,
            "default": None,
            "choices": [],
            "elements": "str",
            "suboptions": {},
        },
        "interface": {
            "type": str,
            "required": True,
            "default": None,
            "choices": [],
        },
        "disabled": {
            "type": bool,
            "required": False,
            "default": False,
            "choices": [],
        },
        "nobinat": {
            "type": bool,
            "required": False,
            "default": False,
            "choices": [],
        },
        "natreflection": {
            "type": str,
            "required": False,
            "default": None,
            "choices": ['enable', 'disable'],
        },
        "ipprotocol": {
            "type": str,
            "required": False,
            "default": 'inet',
            "choices": ['inet', 'inet6'],
        },
        "external": {
            "type": str,
            "required": True,
            "default": None,
            "choices": [],
        },
        "source": {
            "type": str,
            "required": True,
            "default": None,
            "choices": [],
        },
        "destination": {
            "type": str,
            "required": True,
            "default": None,
            "choices": [],
        },
        "descr": {
            "type": str,
            "required": False,
            "default": '',
            "choices": [],
        },
    }

    module = AnsibleModule(
        argument_spec=module_args,
        supports_check_mode=True
    )

    client = rest.RestClient(
        host=module.params['api_host'],
        port=module.params['api_port'],
        username=module.params['api_username'],
        password=module.params['api_password'],
        api_key=module.params['api_key'],
        validate_certs=module.params['validate_certs']
    )

    base_module = base.BaseModule(client)
    changed, data = base_module.set_object_state(
        state=module.params['state'],
        data=module.params,
        lookup_fields=module.params['lookup_fields']
    )

    # If the result was unsuccessful, fail the tasks with the error message returned from the API
    if resp['status'] != 200:
        module.fail_json(msg=resp['message'], **resp)

    result = {'changed': changed, "msg": "Successfully completed API request.", **resp}
    module.exit_json(**result)


if __name__ == '__main__':
    run_module()