#!/usr/bin/python
###############################################################
# DO NOT EDIT THIS FILE DIRECTLY. THIS FILE IS AUTO-GENERATED.
# BY tools/module_generator.py BASED ON THE REST API SCHEMA IF
# YOU NEED TO MAKE CHANGES, PLEASE EDIT THE TEMPLATE OR THE
# GENERATOR SCRIPT. ALTERNATIVELY, YOU CAN OVERRIDE THE AUTO
# GENERATED MODULE BY ADDING THIS MODULES NAME TO THE
# tools/generator.yml FILE.
###############################################################

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.pfrest.pfsense.plugins.module_utils import base, rest

DOCUMENTATION = '''
module: firewall_nat_port_forward
description:
- Manage individual Port Forwards.
short_description: Manage individual Port Forwards.
options:
  api_host:
    type: str
    required: true
    description: The hostname or IP address of the pfSense device.
  api_port:
    type: int
    default: 443
    description: The port number of the pfSense API.
  api_username:
    type: str
    default: admin
    description: The username to authenticate with the pfSense API.
  api_password:
    type: str
    default: pfsense
    no_log: true
    description: The password to authenticate with the pfSense API.
  api_key:
    type: str
    no_log: true
    description: An optional API key for authentication instead of username/password.
  validate_certs:
    type: bool
    default: true
    description: Whether to validate SSL certificates when connecting to the API.
  state:
    type: str
    choices:
    - present
    - absent
    default: present
    description: Whether the resource should be present or absent.
  lookup_fields:
    type: list
    elements: str
    required: true
    description: The list of fields to use when looking up existing resources. This
      should be a list of field names that uniquely identify a resource.
  interface:
    required: true
    type: str
    default: null
    choices: []
    description: The interface this port forward rule applies to.
  ipprotocol:
    required: false
    type: str
    default: inet
    choices:
    - inet
    - inet6
    - inet46
    description: The IP protocol this port forward rule should match.
  protocol:
    required: true
    type: str
    default: null
    choices:
    - any
    - tcp
    - udp
    - tcp/udp
    - icmp
    - esp
    - ah
    - gre
    - ipv6
    - igmp
    - pim
    - ospf
    description: The IP/transport protocol this port forward rule should match.
  source:
    required: true
    type: str
    default: null
    choices: []
    description: 'The source address this port forward rule applies to. Valid value
      options are: an existing interface, an IP address, a subnet CIDR, an existing
      alias, `any`, `(self)`, `l2tp`, `pppoe`. The context of this address can be
      inverted by prefixing the value with `!`. For interface values, the `:ip` modifier
      can be appended to the value to use the interface''s IP address instead of its
      entire subnet.'
  source_port:
    required: false
    type: str
    default: null
    choices: []
    description: 'The source port this port forward rule applies to. Set to `null`
      to allow any source port. Valid options are: a TCP/UDP port number, a TCP/UDP
      port range separated by `:`, an existing port type firewall alias'
  destination:
    required: true
    type: str
    default: null
    choices: []
    description: 'The destination address this rule applies to. Valid value options
      are: an existing interface, an IP address, a subnet CIDR, an existing alias,
      `any`, `(self)`, `l2tp`, `pppoe`. The context of this address can be inverted
      by prefixing the value with `!`. For interface values, the `:ip` modifier can
      be appended to the value to use the interface''s IP address instead of its entire
      subnet.'
  destination_port:
    required: false
    type: str
    default: null
    choices: []
    description: 'The destination port this port forward rule applies to. Set to `null`
      to allow any destination port. Valid options are: a TCP/UDP port number, a TCP/UDP
      port range separated by `:`, an existing port type firewall alias'
  target:
    required: true
    type: str
    default: null
    choices: []
    description: 'The IP address or alias of the internal host to forward matching
      traffic to. Valid value options are: an IP address, an existing alias. For interface
      values, the `:ip` modifier can be appended to the value to use the interface''s
      IP address instead of its entire subnet.'
  local_port:
    required: true
    type: str
    default: null
    choices: []
    description: 'The port on the internal host to forward matching traffic to. In
      most cases, this must match the `destination_port` value. In the event that
      the `desintation_port` is a range, this value should be the first value in that
      range. Valid options are: a TCP/UDP port number, an existing port type firewall
      alias'
  disabled:
    required: false
    type: bool
    default: false
    choices: []
    description: Disables this port forward rule.
  nordr:
    required: false
    type: bool
    default: false
    choices: []
    description: Disables redirection for traffic matching this rule.
  nosync:
    required: false
    type: bool
    default: false
    choices: []
    description: Prevents this port forward rule from being synced to non-primary
      CARP members.
  descr:
    required: false
    type: str
    default: ''
    choices: []
    description: A description for this port forward rule.
  natreflection:
    required: false
    type: str
    default: null
    choices:
    - enable
    - disable
    - purenat
    description: The NAT reflection mode to use for traffic matching this port forward
      rule. Set to `null` to use the system default.
  associated_rule_id:
    required: false
    type: str
    default: ''
    choices: []
    description: The associated firewall rule mode. Use an empty string to require
      a separate firewall rule to be created to pass traffic matching this port forward
      rule. Use `new` to create a new associated firewall rule to pass traffic matching
      this port forward rule. Use `pass` to automatically pass traffic matching this
      port forward rule without the need for a firewall rule. Otherwise, you can specify
      the `associated_rule_id` of an existing firewall rule to associate with this
      port forward rule.
author:
- Jared Hendrickson (@jaredhendrickson13)

'''

EXAMPLES = '''
- name: Create Port Forward
  pfrest.pfsense.firewall_nat_port_forward:
    api_host: pfsense.example.com
    api_username: admin
    api_password: pfsense
    state: present
    interface: example
    protocol: any
    source: example
    destination: example
    target: example
    local_port: example
- name: Delete Port Forward
  pfrest.pfsense.firewall_nat_port_forward:
    api_host: pfsense.example.com
    api_username: admin
    api_password: pfsense
    state: absent
    interface: example
    protocol: any
    source: example
    destination: example
    target: example
    local_port: example

'''

RETURNS = '''
changed:
  description: Whether any changes were made.
  type: bool
  returned: always
status:
  description: The HTTP status code of the API response.
  type: int
  returned: always
response_id:
  description: The unique response/error ID from the API.
  type: str
  returned: always
msg:
  description: A status message from the API.
  type: str
  returned: always
data:
  description: The Port Forward data returned by the API.
  type: dict
  returned: always
  contains:
    interface:
      description: The interface this port forward rule applies to.
      type: str
      returned: always
    ipprotocol:
      description: The IP protocol this port forward rule should match.
      type: str
      returned: always
    protocol:
      description: The IP/transport protocol this port forward rule should match.
      type: str
      returned: always
    source:
      description: 'The source address this port forward rule applies to. Valid value
        options are: an existing interface, an IP address, a subnet CIDR, an existing
        alias, `any`, `(self)`, `l2tp`, `pppoe`. The context of this address can be
        inverted by prefixing the value with `!`. For interface values, the `:ip`
        modifier can be appended to the value to use the interface''s IP address instead
        of its entire subnet.'
      type: str
      returned: always
    source_port:
      description: 'The source port this port forward rule applies to. Set to `null`
        to allow any source port. Valid options are: a TCP/UDP port number, a TCP/UDP
        port range separated by `:`, an existing port type firewall alias'
      type: str
      returned: always
    destination:
      description: 'The destination address this rule applies to. Valid value options
        are: an existing interface, an IP address, a subnet CIDR, an existing alias,
        `any`, `(self)`, `l2tp`, `pppoe`. The context of this address can be inverted
        by prefixing the value with `!`. For interface values, the `:ip` modifier
        can be appended to the value to use the interface''s IP address instead of
        its entire subnet.'
      type: str
      returned: always
    destination_port:
      description: 'The destination port this port forward rule applies to. Set to
        `null` to allow any destination port. Valid options are: a TCP/UDP port number,
        a TCP/UDP port range separated by `:`, an existing port type firewall alias'
      type: str
      returned: always
    target:
      description: 'The IP address or alias of the internal host to forward matching
        traffic to. Valid value options are: an IP address, an existing alias. For
        interface values, the `:ip` modifier can be appended to the value to use the
        interface''s IP address instead of its entire subnet.'
      type: str
      returned: always
    local_port:
      description: 'The port on the internal host to forward matching traffic to.
        In most cases, this must match the `destination_port` value. In the event
        that the `desintation_port` is a range, this value should be the first value
        in that range. Valid options are: a TCP/UDP port number, an existing port
        type firewall alias'
      type: str
      returned: always
    disabled:
      description: Disables this port forward rule.
      type: bool
      returned: always
    nordr:
      description: Disables redirection for traffic matching this rule.
      type: bool
      returned: always
    nosync:
      description: Prevents this port forward rule from being synced to non-primary
        CARP members.
      type: bool
      returned: always
    descr:
      description: A description for this port forward rule.
      type: str
      returned: always
    natreflection:
      description: The NAT reflection mode to use for traffic matching this port forward
        rule. Set to `null` to use the system default.
      type: str
      returned: always
    associated_rule_id:
      description: The associated firewall rule mode. Use an empty string to require
        a separate firewall rule to be created to pass traffic matching this port
        forward rule. Use `new` to create a new associated firewall rule to pass traffic
        matching this port forward rule. Use `pass` to automatically pass traffic
        matching this port forward rule without the need for a firewall rule. Otherwise,
        you can specify the `associated_rule_id` of an existing firewall rule to associate
        with this port forward rule.
      type: str
      returned: always
    created_time:
      description: The unix timestamp of when this port forward rule was original
        created.
      type: int
      returned: always
    created_by:
      description: The username and IP of the user who originally created this port
        forward rule.
      type: str
      returned: always
    updated_time:
      description: The unix timestamp of when this port forward rule was original
        created.
      type: int
      returned: always
    updated_by:
      description: The username and IP of the user who last updated this port forward
        rule.
      type: str
      returned: always

'''


def run_module():
    module_args = {
        "api_host": {
            "type": "str",
            "required": True,
        },
        "api_port": {
            "type": "int",
            "required": False,
            "default": 443,
        },
        "api_username": {
            "type": "str",
            "required": False,
            "default": 'admin',
        },
        "api_password": {
            "type": "str",
            "required": False,
            "default": 'pfsense',
        },
        "api_key": {
            "type": "str",
            "required": False,
        },
        "validate_certs": {
            "type": "bool",
            "required": False,
            "default": True,
        },
        "state": {
            "type": "str",
            "required": False,
            "default": 'present',
            "choices": ['present', 'absent'],
        },
        "lookup_fields": {
            "type": "list",
            "required": True,
            "elements": "str",
        },
        "interface": {
            "type": "str",
            "required": True,
            "default": None,
        },
        "ipprotocol": {
            "type": "str",
            "required": False,
            "default": 'inet',
            "choices": ['inet', 'inet6', 'inet46'],
        },
        "protocol": {
            "type": "str",
            "required": True,
            "default": None,
            "choices": ['any', 'tcp', 'udp', 'tcp/udp', 'icmp', 'esp', 'ah', 'gre', 'ipv6', 'igmp', 'pim', 'ospf'],
        },
        "source": {
            "type": "str",
            "required": True,
            "default": None,
        },
        "source_port": {
            "type": "str",
            "required": False,
            "default": None,
        },
        "destination": {
            "type": "str",
            "required": True,
            "default": None,
        },
        "destination_port": {
            "type": "str",
            "required": False,
            "default": None,
        },
        "target": {
            "type": "str",
            "required": True,
            "default": None,
        },
        "local_port": {
            "type": "str",
            "required": True,
            "default": None,
        },
        "disabled": {
            "type": "bool",
            "required": False,
            "default": False,
        },
        "nordr": {
            "type": "bool",
            "required": False,
            "default": False,
        },
        "nosync": {
            "type": "bool",
            "required": False,
            "default": False,
        },
        "descr": {
            "type": "str",
            "required": False,
            "default": '',
        },
        "natreflection": {
            "type": "str",
            "required": False,
            "default": None,
            "choices": ['enable', 'disable', 'purenat'],
        },
        "associated_rule_id": {
            "type": "str",
            "required": False,
            "default": '',
        },
    }

    module = AnsibleModule(
        argument_spec=module_args,
        supports_check_mode=True
    )

    client = rest.RestClient(
        host=module.params['api_host'],
        port=module.params['api_port'],
        username=module.params['api_username'],
        password=module.params['api_password'],
        api_key=module.params['api_key'],
        validate_certs=module.params['validate_certs']
    )

    base_module = base.BaseModule('/api/v2/firewall/nat/port_forward', client)
    changed, resp = base_module.set_object_state(
        state=module.params['state'],
        data=module.params,
        lookup_fields=module.params['lookup_fields']
    )

    # Capture the response message and clear it (prevent duplicate message/msg in result)
    message = resp.get('message', '')
    if 'message' in resp:
        del resp['message']

    # If the result was unsuccessful, fail the tasks with the error message returned from the API
    if 'code' not in resp or resp['code'] != 200:
        module.fail_json(msg=message, **resp)

    result = {'changed': changed, "msg": "Successfully completed API request.", **resp}
    module.exit_json(**result)


if __name__ == '__main__':
    run_module()