#!/usr/bin/python
###############################################################
# DO NOT EDIT THIS FILE DIRECTLY. THIS FILE IS AUTO-GENERATED.
# BY tools/module_generator.py BASED ON THE REST API SCHEMA IF
# YOU NEED TO MAKE CHANGES, PLEASE EDIT THE TEMPLATE OR THE
# GENERATOR SCRIPT. ALTERNATIVELY, YOU CAN OVERRIDE THE AUTO
# GENERATED MODULE BY ADDING THIS MODULES NAME TO THE
# tools/generator.yml FILE.
###############################################################
"""An Ansible module for interacting with /api/v2/status/wireguard/tunnels."""

# pylint: disable=too-many-lines,duplicate-code,line-too-long

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.pfrest.pfsense.plugins.module_utils import base, rest

DOCUMENTATION = r"""
module: status_wireguard_tunnels_info
description:
- Retrieve information about many WireGuard Tunnel Statuses.
short_description: Retrieve information about many WireGuard Tunnel Statuses.
requirements:
- pfSense-pkg-RESTAPI must be installed on the target system.
- pfSense-pkg-WireGuard must be installed on the target system.
options:
  api_host:
    type: str
    required: true
    description: The hostname or IP address of the pfSense device.
  api_port:
    type: int
    default: 443
    description: The port number of the pfSense API.
  api_protocol:
    type: str
    default: https
    choices:
    - http
    - https
    description: The protocol to use when connecting to the pfSense API.
  api_username:
    type: str
    default: admin
    description: The username to authenticate with the pfSense API.
  api_password:
    type: str
    default: pfsense
    description: The password to authenticate with the pfSense API.
  api_key:
    type: str
    description: An API key to use for authentication.
  validate_certs:
    type: bool
    default: true
    description: Whether to validate SSL certificates when connecting to the API.
  query_params:
    type: dict
    description: Optional query parameters for filtering the results.
author:
- Jared Hendrickson (@jaredhendrickson13)

"""

EXAMPLES = """
- name: Retrieve all WireGuard Tunnel Statuses
  pfrest.pfsense.status_wireguard_tunnels_info:
    api_host: pfsense.example.com
    api_username: admin
    api_password: pfsense

"""

RETURN = """
changed:
  description: Whether any changes were made.
  type: bool
  returned: always
status:
  description: The HTTP status code of the API response.
  type: int
  returned: always
response_id:
  description: The unique response/error ID from the API.
  type: str
  returned: always
msg:
  description: A status message from the API.
  type: str
  returned: always
data:
  description: A list of WireGuard Tunnel Statuses returned by the API.
  type: list
  elements: dict
  returned: always
  contains:
    name:
      description: The WireGuard tunnel interface name (e.g. wg0).
      type: str
      returned: always
    status:
      description: The current operational status of the tunnel interface.
      type: str
      returned: always
    public_key:
      description: The public key of this tunnel.
      type: str
      returned: always
    listen_port:
      description: The UDP port this tunnel is listening on.
      type: str
      returned: always
    mtu:
      description: The MTU of the tunnel interface.
      type: int
      returned: always
    transfer_rx:
      description: The total number of bytes received on the tunnel interface.
      type: int
      returned: always
    transfer_tx:
      description: The total number of bytes transmitted on the tunnel interface.
      type: int
      returned: always
    inpkts:
      description: The total number of inbound packets observed on the tunnel interface.
      type: int
      returned: always
    outpkts:
      description: The total number of outbound packets observed on the tunnel interface.
      type: int
      returned: always
    descr:
      description: The description of this tunnel as stored in the pfSense configuration.
      type: str
      returned: always
    peers:
      description: The live status of each peer associated with this WireGuard tunnel.
      type: list
      returned: always
      elements: dict
      contains:
        tunnel_device:
          description: The tunnel device this peer is associated with.
          type: str
          returned: always
        public_key:
          description: The public key of the peer.
          type: str
          returned: always
        preshared_key:
          description: The preshared key negotiated with this peer, if any.
          type: str
          returned: always
        endpoint:
          description: The remote endpoint (IP:port) the peer is connected from.
          type: str
          returned: always
        allowed_ips:
          description: The comma-separated list of IP prefixes that are routed to
            this peer.
          type: list
          returned: always
          elements: str
        latest_handshake:
          description: The Unix timestamp of the most recent handshake with this peer.
          type: int
          returned: always
        transfer_rx:
          description: The total number of bytes received from this peer.
          type: int
          returned: always
        transfer_tx:
          description: The total number of bytes sent to this peer.
          type: int
          returned: always
        persistent_keepalive:
          description: The persistent keepalive interval for this peer, or "off" if
            disabled.
          type: str
          returned: always
        descr:
          description: The description of this peer as stored in the pfSense configuration.
          type: str
          returned: always

"""


def run_module():
    """Runs this info module against /api/v2/status/wireguard/tunnels."""

    module_args = {
        "api_host": {
            "type": "str",
            "required": True,
            "no_log": False,
        },
        "api_port": {
            "type": "int",
            "required": False,
            "no_log": False,
            "default": 443,
        },
        "api_protocol": {
            "type": "str",
            "required": False,
            "no_log": False,
            "default": "https",
            "choices": ["http", "https"],
        },
        "api_username": {
            "type": "str",
            "required": False,
            "no_log": False,
            "default": "admin",
        },
        "api_password": {
            "type": "str",
            "required": False,
            "no_log": True,
            "default": "pfsense",
        },
        "api_key": {
            "type": "str",
            "required": False,
            "no_log": True,
        },
        "validate_certs": {
            "type": "bool",
            "required": False,
            "no_log": False,
            "default": True,
        },
        "query_params": {
            "type": "dict",
            "required": False,
            "no_log": False,
        },
    }

    module = AnsibleModule(argument_spec=module_args, supports_check_mode=True)

    client = rest.RestClient(
        host=module.params["api_host"],
        port=module.params["api_port"],
        scheme=module.params["api_protocol"],
        username=module.params["api_username"],
        password=module.params["api_password"],
        api_key=module.params.get("api_key", ""),
        validate_certs=module.params["validate_certs"],
    )

    base_module = base.BaseModule("/api/v2/status/wireguard/tunnels", client)
    changed = False
    resp = base_module.lookup_objects(lookup_params=module.params["query_params"])

    # Capture the response message and clear it (prevent duplicate message/msg in result)
    message = resp.get("message", "")
    if "message" in resp:
        del resp["message"]

    # If the result was unsuccessful, fail the tasks with the error message returned from the API
    if "code" not in resp or resp["code"] != 200:
        module.fail_json(msg=message, **resp)

    result = {"changed": changed, "msg": "Successfully completed API request.", **resp}
    module.exit_json(**result)


if __name__ == "__main__":
    run_module()
