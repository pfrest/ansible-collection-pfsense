#!/usr/bin/python
###############################################################
# DO NOT EDIT THIS FILE DIRECTLY. THIS FILE IS AUTO-GENERATED.
# BY tools/module_generator.py BASED ON THE REST API SCHEMA IF
# YOU NEED TO MAKE CHANGES, PLEASE EDIT THE TEMPLATE OR THE
# GENERATOR SCRIPT. ALTERNATIVELY, YOU CAN OVERRIDE THE AUTO
# GENERATED MODULE BY ADDING THIS MODULES NAME TO THE
# tools/generator.yml FILE.
###############################################################

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.pfrest.pfsense.plugins.module_utils import base, rest

DOCUMENTATION = '''
module: vpn_wireguard_settings
description:
- Manage WireGuard Settings.
short_description: Manage WireGuard Settings.
options:
  api_host:
    type: str
    required: true
    description: The hostname or IP address of the pfSense device.
  api_port:
    type: int
    default: 443
    description: The port number of the pfSense API.
  api_username:
    type: str
    default: admin
    description: The username to authenticate with the pfSense API.
  api_password:
    type: str
    default: pfsense
    no_log: true
    description: The password to authenticate with the pfSense API.
  api_key:
    type: str
    no_log: true
    description: An optional API key for authentication instead of username/password.
  validate_certs:
    type: bool
    default: true
    description: Whether to validate SSL certificates when connecting to the API.
  enable:
    required: false
    type: bool
    default: false
    choices: []
    description: Enables or disables WireGuard on this system. WireGuard cannot be
      disabled when one or more tunnels is assigned to a pfSense interface.
  keep_conf:
    required: false
    type: bool
    default: true
    choices: []
    description: Enables or disables keeping the WireGuard configuration when the
      package is uninstalled/reinstalled.
  resolve_interval_track:
    required: false
    type: bool
    default: false
    choices: []
    description: Enables or disables tracking the 'Aliases Hostnames Resolve Interval'
      value as the `resolve_internal` value instead of specifying a value directly.
  resolve_interval:
    required: false
    type: int
    default: 300
    choices: []
    description: The interval (in seconds) for re-resolving endpoint host/domain names.
  interface_group:
    required: false
    type: str
    default: all
    choices:
    - all
    - unassigned
    - none
    description: Configures which WireGuard tunnels are members of the WireGuard interface
      group.
  hide_secrets:
    required: false
    type: bool
    default: false
    choices: []
    description: Enables or disables hiding all secrets (private and pre-shared keys)
      in the user interface.
  hide_peers:
    required: false
    type: bool
    default: false
    choices: []
    description: Enables or disables initially hiding all peers in the user interface.
author:
- Jared Hendrickson (@jaredhendrickson13)

'''

EXAMPLES = '''
- name: Manage WireGuard Settings
  pfrest.pfsense.vpn_wireguard_settings:
    api_host: pfsense.example.com
    api_username: admin
    api_password: pfsense
    enable: false
    keep_conf: true
    resolve_interval_track: false

'''

RETURNS = '''
changed:
  description: Whether any changes were made.
  type: bool
  returned: always
status:
  description: The HTTP status code of the API response.
  type: int
  returned: always
response_id:
  description: The unique response/error ID from the API.
  type: str
  returned: always
msg:
  description: A status message from the API.
  type: str
  returned: always
data:
  description: The WireGuard Settings data returned by the API.
  type: dict
  returned: always
  contains:
    enable:
      description: Enables or disables WireGuard on this system. WireGuard cannot
        be disabled when one or more tunnels is assigned to a pfSense interface.
      type: bool
      returned: always
    keep_conf:
      description: Enables or disables keeping the WireGuard configuration when the
        package is uninstalled/reinstalled.
      type: bool
      returned: always
    resolve_interval_track:
      description: Enables or disables tracking the 'Aliases Hostnames Resolve Interval'
        value as the `resolve_internal` value instead of specifying a value directly.
      type: bool
      returned: always
    resolve_interval:
      description: The interval (in seconds) for re-resolving endpoint host/domain
        names.
      type: int
      returned: always
    interface_group:
      description: Configures which WireGuard tunnels are members of the WireGuard
        interface group.
      type: str
      returned: always
    hide_secrets:
      description: Enables or disables hiding all secrets (private and pre-shared
        keys) in the user interface.
      type: bool
      returned: always
    hide_peers:
      description: Enables or disables initially hiding all peers in the user interface.
      type: bool
      returned: always

'''


def run_module():
    module_args = {
        "api_host": {
            "type": "str",
            "required": True,
        },
        "api_port": {
            "type": "int",
            "required": False,
            "default": 443,
        },
        "api_username": {
            "type": "str",
            "required": False,
            "default": 'admin',
        },
        "api_password": {
            "type": "str",
            "required": False,
            "default": 'pfsense',
        },
        "api_key": {
            "type": "str",
            "required": False,
        },
        "validate_certs": {
            "type": "bool",
            "required": False,
            "default": True,
        },
        "enable": {
            "type": "bool",
            "required": False,
            "default": False,
        },
        "keep_conf": {
            "type": "bool",
            "required": False,
            "default": True,
        },
        "resolve_interval_track": {
            "type": "bool",
            "required": False,
            "default": False,
        },
        "resolve_interval": {
            "type": "int",
            "required": False,
            "default": 300,
        },
        "interface_group": {
            "type": "str",
            "required": False,
            "default": 'all',
            "choices": ['all', 'unassigned', 'none'],
        },
        "hide_secrets": {
            "type": "bool",
            "required": False,
            "default": False,
        },
        "hide_peers": {
            "type": "bool",
            "required": False,
            "default": False,
        },
    }

    module = AnsibleModule(
        argument_spec=module_args,
        supports_check_mode=True
    )

    client = rest.RestClient(
        host=module.params['api_host'],
        port=module.params['api_port'],
        username=module.params['api_username'],
        password=module.params['api_password'],
        api_key=module.params['api_key'],
        validate_certs=module.params['validate_certs']
    )

    base_module = base.BaseModule('/api/v2/vpn/wireguard/settings', client)

    # Capture the response message and clear it (prevent duplicate message/msg in result)
    message = resp.get('message', '')
    if 'message' in resp:
        del resp['message']

    # If the result was unsuccessful, fail the tasks with the error message returned from the API
    if 'code' not in resp or resp['code'] != 200:
        module.fail_json(msg=message, **resp)

    result = {'changed': changed, "msg": "Successfully completed API request.", **resp}
    module.exit_json(**result)


if __name__ == '__main__':
    run_module()