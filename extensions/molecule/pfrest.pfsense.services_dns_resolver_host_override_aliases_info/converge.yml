---
- name: Test pfrest.pfsense.services_dns_resolver_host_override_aliases_info module
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Retrieve DNS Resolver Host Override Aliases
      register: result
      pfrest.pfsense.services_dns_resolver_host_override_aliases_info:
        api_host: localhost
        api_port: 5000
        api_protocol: http
        validate_certs: false

    - name: Assert results
      ansible.builtin.assert:
        that:
          - result is defined
          - result is mapping
          - result.code is defined
          - result.code == 200
          - result.response_id is defined
          - result.response_id == "MOCK_SUCCESS"
          - result.msg is defined
          - result.data is defined
          - result.data is sequence
          - 'result.data | length > 0'
          - result.data[0].host is defined
          - result.data[0].domain is defined
          - result.data[0].descr is defined
          - result.data[0].id is defined

