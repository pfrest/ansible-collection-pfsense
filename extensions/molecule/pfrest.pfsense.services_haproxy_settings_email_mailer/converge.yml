---
- name: Test pfrest.pfsense.services_haproxy_settings_email_mailer module
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Manage HA Proxy Email Mailer
      register: result
      pfrest.pfsense.services_haproxy_settings_email_mailer:
        api_host: localhost
        api_port: 5000
        api_protocol: http
        validate_certs: false
        state: present
        lookup_fields:
          - name
        name: string
        mailserver: string
        mailserverport: string
        parent_lookup_fields:
          - id
        id: 0

    - name: Assert results
      ansible.builtin.assert:
        that:
          - result is defined
          - result is mapping
          - result.code is defined
          - result.code == 200
          - result.response_id is defined
          - result.response_id == "MOCK_SUCCESS"
          - result.msg is defined
          - result.data is defined
          - result.data is mapping
          - result.data.name is defined
          - result.data.mailserver is defined
          - result.data.mailserverport is defined
          - result.data.id is defined

