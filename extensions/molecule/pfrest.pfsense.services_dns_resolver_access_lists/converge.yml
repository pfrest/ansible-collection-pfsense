---
- name: Test pfrest.pfsense.services_dns_resolver_access_lists module
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Manage all DNS Resolver Access Lists
      register: result
      pfrest.pfsense.services_dns_resolver_access_lists:
        api_host: localhost
        api_port: 15070
        api_protocol: http
        validate_certs: false
        objects:
          - name: string
            action: allow
            description: string
            networks:
              - network: string
                mask: 9223372036854776000
                description: string

    - name: Assert results
      ansible.builtin.assert:
        that:
          - result is defined
          - result is mapping
          - result.code is defined
          - result.code == 200
          - result.response_id is defined
          - result.response_id == "MOCK_SUCCESS"
          - result.msg is defined
          - result.data is defined
          - result.data is sequence

