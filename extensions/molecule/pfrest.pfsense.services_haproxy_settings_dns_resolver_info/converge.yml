---
- name: Test pfrest.pfsense.services_haproxy_settings_dns_resolver_info module
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Read HA Proxy DNS Resolver
      register: result
      pfrest.pfsense.services_haproxy_settings_dns_resolver_info:
        api_host: localhost
        api_port: 5000
        api_protocol: http
        validate_certs: false
        lookup_params: {}

    - name: Assert results
      ansible.builtin.assert:
        that:
          - result is defined
          - result is mapping
          - result.code is defined
          - result.code == 200
          - result.response_id is defined
          - result.response_id == "MOCK_SUCCESS"
          - result.msg is defined
          - result.data is defined
          - result.data is mapping
          - result.data.name is defined
          - result.data.name == "string"
          - result.data.server is defined
          - result.data.server == "string"
          - result.data.port is defined
          - result.data.port == "string"
          - result.data.id is defined
          - result.data.id == 0

