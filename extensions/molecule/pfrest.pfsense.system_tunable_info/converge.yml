---
- name: Test pfrest.pfsense.system_tunable_info module
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Read System Tunable
      register: result
      pfrest.pfsense.system_tunable_info:
        api_host: localhost
        api_port: 15070
        api_protocol: http
        validate_certs: false
        lookup_params: {}

    - name: Assert results
      ansible.builtin.assert:
        that:
          - result is defined
          - result is mapping
          - result.code is defined
          - result.code == 200
          - result.response_id is defined
          - result.response_id == "MOCK_SUCCESS"
          - result.msg is defined
          - result.data is defined
          - result.data is mapping
          - result.data.tunable is defined
          - result.data.tunable == "string"
          - result.data.value is defined
          - result.data.value == "string"
          - result.data.descr is defined
          - result.data.descr == "string"
          - result.data.id is defined
          - result.data.id == 0

